import {test} from 'node:test';import assert from 'node:assert/strict';import {matchEvent,playerError} from '../lib/football/matching.ts';
const m={id:'fd:123',league:'fr.1',source:'football-data.org',utc:'2026-09-18T19:00:00Z',home:'Example FC',away:'Other CF'};const e={home_team:'Example',away_team:'Other',commence_time:m.utc};
test('event association requires competition, kickoff, ordered teams and uniqueness',()=>{assert.equal(matchEvent(e,'fr.1',[m])?.id,m.id);assert.equal(matchEvent(e,'en.1',[m]),null);assert.equal(matchEvent({...e,commence_time:'2026-09-18T20:00:00Z'},'fr.1',[m]),null);assert.equal(matchEvent({...e,home_team:'Other',away_team:'Example'},'fr.1',[m]),null);assert.equal(matchEvent(e,'fr.1',[m,m]),null);assert.equal(matchEvent(e,'fr.1',[{...m,utc:null}]),null)});
test('provider errors classified without echoing provider data or secrets',()=>{assert.match(playerError({plan:'Free plans do not have access to this season'}),/Saison inaccessible/);assert.ok(!playerError({secret:'example-private-key'}).includes('example-private-key'))});
