import {test} from 'node:test';import assert from 'node:assert/strict';import {bestQuote,freshQuote,prospectiveMetrics,statusLabel} from '../lib/football/presentation.ts';
test('a fresh price takes priority over a stale high price; markets and totals stay separate',()=>{const now=Date.parse('2026-09-19T12:00:00Z');const q={market:'h2h',outcome:'home',price:2,updatedAt:'2026-09-19T11:00:00Z'};assert.equal(bestQuote([q,{...q,price:4,updatedAt:'2026-09-18T12:00:00Z'}],'h2h','home',undefined,now).price,2);assert.equal(bestQuote([q],'totals','Over',2.5,now),null);assert.equal(freshQuote({...q,updatedAt:'invalid'},now),false);assert.equal(freshQuote({...q,updatedAt:'2026-09-20T12:00:00Z'},now),false);assert.equal(statusLabel('IN_PLAY'),'En cours')});
test('prospective scores reject postmatch, invalid dates and missing results',()=>{const row={published_at:'2026-09-18T10:00:00Z',kickoff:'2026-09-18T18:00:00Z',payload:JSON.stringify({probabilities:{home:.6,draw:.2,away:.2}}),result:JSON.stringify({hg:2,ag:0})};const report=prospectiveMetrics([row,{...row,published_at:'2026-09-19T00:00:00Z'},{...row,kickoff:'invalid'},{...row,result:null}]);assert.equal(report.n,1);assert.ok(Math.abs(report.brier-.24)<1e-12);assert.equal(prospectiveMetrics([]).brier,null)});
