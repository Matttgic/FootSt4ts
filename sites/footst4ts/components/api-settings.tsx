'use client';
import {useEffect,useState} from 'react';
const providers=[['FOOTBALL_DATA_KEY','football-data.org','https://www.football-data.org/client/register'],['ODDS_API_KEY','The Odds API','https://the-odds-api.com'],['API_FOOTBALL_KEY','API-Football','https://dashboard.api-football.com/register']];
export default function ApiSettings(){const [state,setState]=useState<any>(null),[values,setValues]=useState<Record<string,string>>({}),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function load(){try{const r=await fetch('/api/settings',{cache:'no-store'});setState(await r.json())}catch{setState({error:'Connexion indisponible.'})}}useEffect(()=>{load()},[]);async function send(body:unknown){setBusy(true);setMessage('');try{const r=await fetch('/api/settings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d:any=await r.json();setMessage(d.error||d.message);if(r.ok){setValues({});await load()}}catch{setMessage('Connexion interrompue. Réessayez.')}finally{setBusy(false)}}return <section><h3>Mes clés API · espace privé</h3><p className="source-note">Clés chiffrées côté serveur. Ne les envoyez ni dans le chat ni sur GitHub.</p>{!state?<p>Chargement…</p>:state.error?<p role="alert">{state.error}</p>:<>{providers.map(([name,label,url])=><form key={name} onSubmit={e=>{e.preventDefault();send({name,value:values[name]})}} style={{margin:'20px 0'}}><label htmlFor={name}><strong>{label}</strong></label><p className="source-note">{state.configured[name]?'Clé enregistrée · accès fournisseur à vérifier':'Aucune clé enregistrée'} · <a className="source-link" href={url} target="_blank" rel="noreferrer">Obtenir une clé</a></p><input id={name} type="password" autoComplete="off" spellCheck={false} value={values[name]||''} placeholder="Coller la clé ici" onChange={e=>setValues({...values,[name]:e.target.value})} style={{width:'100%',minWidth:0,minHeight:46,padding:12,background:'#101720',border:'1px solid #536171',borderRadius:8,color:'white'}}/><div style={{display:'flex',gap:8,marginTop:8,flexWrap:'wrap'}}><button className="ui-button" disabled={busy||!values[name]?.trim()||!state.ready}>Enregistrer</button>{state.configured[name]&&<button type="button" className="ui-button" disabled={busy} onClick={()=>send({name,value:''})}>Supprimer</button>}</div></form>)}<label className="small" style={{display:'flex',gap:12,margin:'20px 0'}}><input type="checkbox" disabled={busy} checked={!!state.configured.API_FOOTBALL_RIGHTS_CONFIRMED} onChange={e=>send({name:'API_FOOTBALL_RIGHTS_CONFIRMED',value:e.target.checked?'true':''})}/>Je confirme disposer des droits de réutilisation API-Football pour ce site.</label><button className="ui-button" disabled={busy} onClick={()=>send({action:'collect'})}>{busy?'Traitement…':'Collecter les cotes et joueurs'}</button><p className="source-note">Budgets gratuits respectés. Le calendrier utilise la clé au prochain renouvellement du cache. Les saisons joueurs dépendent de votre offre. Les cotes restent invisibles tant que les identifiants des matchs ne sont pas associés.</p></>}<p role="status" className="small" style={{margin:'12px 0'}}>{message}</p></section>}
