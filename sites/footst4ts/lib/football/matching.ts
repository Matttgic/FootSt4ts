import type {Match} from './core';
// No fuzzy matching: ordered teams, exact UTC kickoff, competition and a unique candidate.
export function teamLabel(name:string){return name.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/\b(fc|cf|afc|sc|ac|ssc|1900|1901|1905|1909|1910|1899)\b/g,'').replace(/[^a-z0-9]/g,'')}
export function matchEvent(event:any,league:string,matches:Match[]){if(!event?.commence_time||!event.home_team||!event.away_team)return null;const rows=matches.filter(m=>m.league===league&&m.source==='football-data.org'&&m.utc&&Date.parse(m.utc)===Date.parse(event.commence_time)&&[m.home,...m.homeAliases??[]].some(n=>teamLabel(n)===teamLabel(event.home_team))&&[m.away,...m.awayAliases??[]].some(n=>teamLabel(n)===teamLabel(event.away_team)));return rows.length===1?rows[0]:null}
export function playerError(errors:unknown){const text=JSON.stringify(errors).toLowerCase();if(/free.*season|season.*free|free plans/.test(text))return 'Saison inaccessible avec cette offre gratuite. Choisissez une saison autorisée dans votre compte API-Football.';if(/token|key|auth/.test(text))return 'Clé API-Football refusée. Vérifiez une clé API-Sports directe, pas RapidAPI.';if(/limit|quota|request/.test(text))return 'Limite API-Football atteinte. Attendez le renouvellement du quota.';return 'API-Football refuse cette saison ou ces données. Vérifiez la couverture de votre offre.'}
