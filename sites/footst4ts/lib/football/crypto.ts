export async function seal(value:string,master:string,name:string){const key=await crypto.subtle.importKey('raw',Uint8Array.from(atob(master),c=>c.charCodeAt(0)),'AES-GCM',false,['encrypt']);const iv=crypto.getRandomValues(new Uint8Array(12));const data=await crypto.subtle.encrypt({name:'AES-GCM',iv,additionalData:new TextEncoder().encode(name)},key,new TextEncoder().encode(value));return JSON.stringify({iv:btoa(String.fromCharCode(...iv)),data:btoa(String.fromCharCode(...new Uint8Array(data)))})}
export async function unseal(value:string,master:string,name:string){const v=JSON.parse(value);const bytes=(s:string)=>Uint8Array.from(atob(s),c=>c.charCodeAt(0));const key=await crypto.subtle.importKey('raw',bytes(master),'AES-GCM',false,['decrypt']);return new TextDecoder().decode(await crypto.subtle.decrypt({name:'AES-GCM',iv:bytes(v.iv),additionalData:new TextEncoder().encode(name)},key,bytes(v.data)))}
